<%= render 'load_repos_button' %>
<table class="table table-striped table-hover table-responsive table-condensed repo-list">
  <tr>
    <th>Repo Name</th>
    <th>Description</th>
    <th>Branch</th>
    <th>Private</th>
    <th></th>
  </tr>
  <% @repos.each do |repo| %>
    <tr>
      <td class="no-wrap">
        <%= repo.name %>
      </td>
      <td><%= repo.description %></td>
      <td><%= repo.master_branch %></td>
      <td><%= repo.private %></td>
      <td>
        <%= render 'book_add_button', repo: repo, user: user, repo_book_ids: @repo_book_ids %>
      </td>
    </tr>
  <% end %>
</table>
