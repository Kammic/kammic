<div class="row row-offcanvas row-offcanvas-right">
  <div class="col-sm-12">
    <div class="row">
      <div class="col-sm-1"></div>
      <div class="col-sm-4">
        <div class="row">
          <%= book_image(@book) %>
          <br/>
          <%= refresh_manifest_button(@book) %>
          <%= unfollow_button(@book) %>
          <%= edit_book_button(@book) %>
          <%= build_button(@book) %>
        </div>
       <div class="col-sm-4">
        <div class="row">
          <%= render 'shared/builds/builds_list_abbr' %>
        </div>
      </div>

     </div>
      <div class="col-sm-3">
        <div class="page-header">
          <h1><%= @book.manifest.title %></h1>
        </div>
        <div class="row">
          <%= book_pages @book.manifest.pages %>
        </div>
      </div>

    </div>
  </div>

</div>
